# Sidebar navigation config per curriculum section.
# modules: data file key (without .yaml) containing modules list
# order: flat topic order file key (used when modules absent or for prev/next)
# flatGroups: optional labeled groups of topic slugs (system-design)
sections:
  system-design:
    order: system_design_order
    flatGroups:
      - id: 1
        label: "System Fundamentals"
        splitAfter: 19
      - id: 2
        label: "Case Studies"
  microservices:
    modules: microservices_modules
    order: microservices_order
  technology-playbook:
    modules: technology_playbook_modules
    order: technology_playbook_order
  java-engineering:
    modules: java_engineering_modules
    order: java_engineering_order
  spring-boot:
    modules: spring_boot_modules
    order: spring_boot_order
  kafka-handbook:
    modules: kafka_handbook_modules
    order: kafka_handbook_order
  kubernetes-handbook:
    modules: kubernetes_handbook_modules
    order: kubernetes_handbook_order
  database-handbook:
    modules: database_handbook_modules
    order: database_handbook_order
  cloud-handbook:
    modules: cloud_handbook_modules
    order: cloud_handbook_order
  design-patterns:
    modules: design_patterns_modules
    order: design_patterns_order
  dsa-coding:
    order: dsa_coding_order
  interview-prep:
    modules: interview_prep_modules
    order: interview_prep_order
  ai-for-engineers:
    modules: ai_for_engineers_modules
    order: ai_for_engineers_order
  security-architecture:
    order: security_architecture_order
